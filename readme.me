Simple weather app with NodeJS on terminal.

You must have NodeJS installed.
Clone repository and in the repository folder run:
npm install

node index.js -c cityToSearch;

Example:
node index.js -c san diego;

